stages:
  - build
  - deploy

build:
  stage: build
  image: node:20-bullseye
  tags:
    - trainer
  script:
    - echo "🚀 Starting build process..."
    - apt-get update -qq && apt-get install -y -qq zip
    
    # Cài đặt dependencies với cách tiếp cận để giải quyết xung đột React 19
    - echo "📦 Installing dependencies..."
    - |
      # Tạo .npmrc để bỏ qua kiểm tra peer dependencies
      echo "legacy-peer-deps=true" > .npmrc
      echo "strict-peer-dependencies=false" >> .npmrc
      
      # Kiểm tra phiên bản React trong package.json
      REACT_VERSION=$(node -e "try { const pkg = require('./package.json'); console.log(pkg.dependencies.react || 'không có'); } catch (e) { console.log('lỗi đọc package.json'); }")
      echo "Phiên bản React trong package.json: $REACT_VERSION"
      
      # Cài đặt React Day Picker với phiên bản phù hợp
      npm install react-day-picker@8.10.1 --ignore-scripts
      
      # Sử dụng npm install với --force và --ignore-scripts
      echo "Cài đặt tất cả dependencies với --force và --ignore-scripts"
      npm install --force --ignore-scripts
      
      # Vá react-day-picker để chấp nhận React 19
      echo "Vá file node_modules/react-day-picker/package.json để chấp nhận React 19"
      node -e "
        const fs = require('fs');
        try {
          const packagePath = './node_modules/react-day-picker/package.json';
          let pkg = JSON.parse(fs.readFileSync(packagePath, 'utf8'));
          if (pkg.peerDependencies && pkg.peerDependencies.react) {
            pkg.peerDependencies.react = '^16.8.0 || ^17.0.0 || ^18.0.0 || ^19.0.0';
            fs.writeFileSync(packagePath, JSON.stringify(pkg, null, 2));
            console.log('Đã vá thành công react-day-picker để chấp nhận React 19');
          }
        } catch (error) {
          console.error('Lỗi khi vá file:', error);
        }
      "
    
    # Biến môi trường quan trọng để tắt LightningCSS
    - export NEXT_DISABLE_LIGHTNINGCSS=1
    - export NODE_OPTIONS="--max-old-space-size=4096"
    - export NODE_ENV=production
    
    # Tạo file .env từ biến môi trường
    - echo "🔧 Configuring environment..."
    - echo "$STAGING_ENV" > .env
    - cat .env
    
    # Sử dụng file next.config.ts hiện có - không tạo file mới
    
    # Build ứng dụng
    - npm run build
    
    # Tạo file zip
    - cd dist
    - zip -r ../deploy.zip .
    - cd ..
  artifacts:
    paths:
      - deploy.zip
  only:
    - staging

deploy:
  stage: deploy
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  tags:
    - trainer
  script:
    - echo "🌐 Starting deployment to AWS Amplify..."
    
    # Cấu hình AWS
    - aws configure set aws_access_key_id "$AWS_ACCESS_KEY_ID"
    - aws configure set aws_secret_access_key "$AWS_SECRET_ACCESS_KEY"
    - aws configure set region "$STAGING_AWS_REGION"
    
    # Kiểm tra xác thực
    - aws sts get-caller-identity
    
    # Deploy lên Amplify
    - aws amplify start-deployment --app-id "$STAGING_AMPLIFY_APP_ID" --branch-name "$STAGING_BRANCH" --zip-file fileb://deploy.zip
    
    # Kiểm tra trạng thái
    - sleep 5
    - aws amplify list-jobs --app-id "$STAGING_AMPLIFY_APP_ID" --branch-name "$STAGING_BRANCH" --max-results 1
  only:
    - staging
  dependencies:
    - build